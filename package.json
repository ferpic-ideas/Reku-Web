{
  "name": "reku-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'Static site; no build step'",
    "start": "node server.mjs",
    "check": "node --check server.mjs && find src -name '*.mjs' -print0 | xargs -0 -n1 node --check && node --check admin/app.js && npm run secrets:check",
    "secrets:check": "bash scripts/secrets_check.sh"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "pg": "^8.22.0",
    "qrcode": "^1.5.4"
  }
}
